<p style="color: green"><%= notice %></p>

<h1>Tasks</h1>

<div id="tasks">
  <% @tasks.each do |task| %>
    <%= render task %>
    <p>
      <%= link_to "Show this task", project_task_path(id: task.id) %>
    </p>
  <% end %>
</div>

<%= link_to "New task", new_project_task_path %>
